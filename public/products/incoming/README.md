# NOVA Product Inbox Output

Images placed in the local inbox are copied here by the self-hosted `nova-local` runner.

Public URL pattern:
`https://raw.githubusercontent.com/aelkhoreiby/nova-watch/main/public/products/incoming/<slug>/<slug>.<ext>`

These URLs are passed to EasyOrders as `thumb` and `images`.
